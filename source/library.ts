LIBRARY({
  name: "NaturePlants",
  version: 1,
  shared: true,
  api: "CoreEngine",
});

const LIB = { name: "NaturePlants", version: 1 };

alert(`${LIB.name} of version ${LIB.version} has been initialized!`)